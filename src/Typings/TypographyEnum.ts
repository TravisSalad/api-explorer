// Note I only added necessary typeography, however, this could be extended to support all html text tags.
export enum TypographyEnum {
  title = 'title',
  subtitle = 'subtitle',
  paragraph = 'paragraph',
}